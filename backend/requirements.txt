# ATRT Drug Repurposing Pipeline — Python Dependencies
# All packages are open-source.
# Tested with Python 3.11.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── Async HTTP (pipeline API calls: OpenTargets, STRING-DB, clue.io) ─────────
aiohttp==3.10.5
certifi==2024.8.30        # up-to-date SSL certificates
requests==2.32.3          # synchronous fallback (prepare_cmap_query.py MyGene.info)

# ── Data processing ───────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
pydantic==2.9.2
python-dotenv==1.0.1

# ── Scientific computing ──────────────────────────────────────────────────────
scipy==1.14.1
scikit-learn==1.5.2       # calibration.py — LogisticRegression, IsotonicRegression
                          # NOTE: import as 'from sklearn...' not 'from scikit_learn'

# ── Graph analysis (PPI network) ──────────────────────────────────────────────
networkx==3.3

# ── DepMap HDF5 / CMap GCTX files ────────────────────────────────────────────
h5py==3.12.1              # for reading .gctx files (CMap L1000)
# cmapPy                  # optional — only needed for local .gctx mode
#                         # install separately: pip install cmapPy

# ── GEO data download (optional — alternative to manual download) ─────────────
# GEOparse==2.0.3         # uncomment to enable automatic GSE70678 download
#                         # install separately if needed

# ── Caching and storage ───────────────────────────────────────────────────────
aiosqlite==0.20.0
sqlalchemy==2.0.36

# ── Figures (generate_figures.py) ────────────────────────────────────────────
matplotlib==3.9.2

# ── Anthropic API (AI explanations, optional) ────────────────────────────────
anthropic==0.40.0

# ── Retry logic ──────────────────────────────────────────────────────────────
tenacity==8.5.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2             # used by FastAPI TestClient

# ── NOT included (intentionally) ─────────────────────────────────────────────
# rdkit — heavy dependency, not required by current pipeline code.
#         Install separately with: conda install -c rdkit rdkit
#         or: pip install rdkit-pypi